# YNX 公开测试网完整验收报告 — 2026-05-01

状态：严格公开测试网验收通过
生成时间：2026-05-01
规范语言：中文

## 总结

YNX 公开测试网当前可以用于开发者接入、演示、API 集成、Web4 流程测试、AI 结算流程测试。

当前测试网已经通过严格公开测试网验收门槛：

- 公共 RPC `/net_info`：`n_peers=3`；
- 当前验证人集合：`4` 个 bonded validators；
- strict readiness：`27 PASS`，`0 WARN`，`0 FAIL`。

## 已验证可用的服务

已通过：

- 文档验收：`13/13 PASS`；
- 公网运行证据：`20/20 PASS`；
- strict extreme readiness：`27 PASS`，`0 WARN`，`0 FAIL`；
- RPC：在线，chain ID 正确，区块高度持续增长；
- EVM JSON-RPC：在线，`eth_chainId=0x238e`；
- REST：在线，chain ID 正确；
- Faucet：健康检查在线；
- Indexer：health、overview、network descriptor、validator endpoint 在线；
- Explorer：config endpoint 在线；
- AI Gateway：health / ready 在线，policy enforcement 已开启；
- Web4 Hub：health / ready 在线，policy enforcement 和 internal authorization 已开启。

运行样本：

- chain ID：`ynx_9102-1`；
- EVM chain ID：`0x238e`；
- track：`v2-web4`；
- 区块增长：`8s` 内增长 `12` 个块；
- 验证人签名：`4/4`。

## 已验证写路径

公网 HTTPS 写路径烟测通过：

- Web4 policy 创建；
- Web4 session 签发；
- wallet bootstrap / verify；
- 无 session 时 policy enforcement 拒绝；
- AI vault 创建；
- AI job 创建、commit、finalize；
- AI payout payment 创建；
- 直接 machine-payment charge；
- x402 未支付返回 `402`，支付后可访问资源；
- identity 创建；
- agent 创建；
- agent self-update；
- agent replicate；
- intent 创建、claim、challenge、finalize；
- policy pause / resume。

最新烟测 ID：

- policy：`policy_a4655a53d91d7741`；
- vault：`vault_2380906d8211982a`；
- job：`job_efa0ebee9426478f`；
- payment：`pay_f2e38489e85d558b`；
- identity：`identity_1812236bbd87889d`；
- agent：`agent_ff4e306c6e5007d9`；
- child agent：`agent_11a5f3bbfc3ba50f`；
- intent：`intent_12ba9890dfe23fcc`。

## 本地测试结果

通过：

- 仓库根目录 `npm test`：contracts + SDK 测试通过；
- 仓库根目录 `npm run lint`：contracts + SDK lint 通过；
- `infra/web4-hub` 的 `npm test` 通过；
- `infra/ai-gateway` 的 `npm test` 通过；
- `chain` 下 `CGO_ENABLED=0 go test $(go list ./... | grep -v '/third_party/')` 通过；
- `./scripts/verify_docs_readiness.sh` 通过；
- `./scripts/prepare_audit_compliance_pack.sh --skip-runtime-evidence` 通过。

环境说明：

- 直接运行不带 `CGO_ENABLED=0` 的 `go test` 会被本机 Xcode license prompt 阻断，不是 YNX 代码失败。

## 当前验证人 / Peer 拓扑

当前公开网络由 1 台 canonical 公共服务节点和 3 台腾讯云验证人 peer 组成：

- `43.153.202.237`：canonical 公共服务节点，moniker `ynx-v2-web4`，node id `7b8bf4128aeb20e12648086a9fa9b6c4a28cb4e7`；
- `43.162.100.54`：硅谷验证人 peer，moniker `ynx-tencent-sv`，node id `aac4cf1eff04ea0bbfdb0762808553ef820d16d2`；
- `43.164.132.81`：首尔验证人 peer，moniker `ynx-tencent-seoul`，node id `ca7292a1529787d34983158934db8c29162d0060`；
- `43.134.23.58`：新加坡验证人 peer，moniker `ynx-tencent-singapore-peer`，node id `c3fdd22c9df6c26dc9cbad88c65c5a1fb1cf0598`。

运维说明：

- 四台节点均使用 TCP `36656` 作为 P2P 端口。
- join/bootstrap 节点必须保持 CometBFT PEX 开启，因为 canonical public peer 会宣告 PEX channel。
- 三台辅助验证人从 canonical data snapshot 初始化，再通过 `MsgCreateValidator` 和追加委托进入 bonded validator set。

## 行业级分类

- 服务可用性：PASS；
- Web4 / AI 写路径：PASS；
- 文档就绪：PASS；
- 本地代码测试：PASS；
- P2P 冗余：PASS；
- 验证人拓扑冗余：PASS。
- 验证人独立性强 claim：本报告不作结论。

总体结论：公开测试网通过当前严格验收门槛。

严格验收命令：

```bash
./scripts/public_testnet_extreme_readiness.sh
```

最新 strict 结果：

- `public_p2p_peers`：`n_peers=3`，最低要求 `2`；
- `validator_set_size`：`validators=4`，最低要求 `4`；
- `validator_signing`：`signed=4/4`。
