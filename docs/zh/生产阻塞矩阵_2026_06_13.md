# YNX 生产阻塞矩阵

状态日期：2026-06-13

## 当前生产状态

- 验证人 bonded：`4/4`
- 公网 readiness gate：`PASS`
- bridge full-loop-tested：`5/5`
- automatic-loop-ready：`4/5`
- automatic-loop-observed：`2/5`

重要解释：

- `4/4 bonded` 本身不等于强验证人独立性；
- `5/5 full-loop-tested` 不等于 `5/5 production-safe`；
- `automatic_loop_ready` 表示 route 配置和 adapter 已具备；
- `automatic_loop_observed` 表示已经有真实 watcher / release 证据，不只是配置到位；
- 这份矩阵应被理解成 testnet 运行证据，而不是“已经机构级”的证明。

## 剩余 route 阻塞项

| Route | 当前状态 | 缺失的生产输入 | 为什么被卡住 |
|---|---|---|---|
| `btc-testnet-btc` | 已 full-loop-tested，automatic-ready，但自动化实证还偏少 | 更多真实 automatic deposit/release 证据 | BTC watcher 和 release adapter 已配置，但公开证据强度还不如 Sepolia 路线 |
| `bnb-testnet-bnb` | 已 full-loop-tested，未 automatic | BSC lockbox 部署、deployer 的 testnet BNB 资金 | EVM lockbox 自动化路径已经有了，但当前 deploy signer 在 BSC testnet 上 gas 为 0，导致 lockbox 还没法发部署交易 |
| `tron-shasta-usdt` | 已 full-loop-tested，automatic-ready，但自动化实证还偏少 | 更多真实 automatic deposit/release 证据 | TRON watcher/release adapter 已配置，但公开证据强度还不如 Sepolia 路线 |

## 这次已经完成的事

- 把验证人集合恢复到 `4/4` bonded
- 把生产 acceptance 跑到 `public_security_gate` 和 `public_testnet_extreme_readiness` 全绿
- 把 `5/5 full-loop-tested` 的对外口径和真实 route 证据对齐
- 把官网生产修复和 live ops 可视化发布到 `https://www.ynxweb4.com`

## 除 route 之外仍然存在的阻塞

- validator 独立性证据还不足，不能激进地使用 decentralization wording；
- 安全流程在改进，但公开安全联系和 incident ownership 还不够组织化；
- infra 的持久化和恢复能力还需要继续硬化，才能接近 production-grade 表述；
- bridge 仍然是整个栈里最高风险、最高信任成本的部分。

## 适合对外的固定说法

建议统一用这句：

`YNX Web4 public testnet is live. We have 5/5 bridge routes full-loop tested, 4/5 routes automatic-loop-ready by current route configuration, and the strongest observed automatic public evidence today on the Sepolia ETH and USDC routes. The bridge should still be described as testnet architecture evidence rather than production custody infrastructure.`
